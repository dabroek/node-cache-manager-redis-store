{
  "name": "@myrror-security/cache-manager-redis-store",
  "author": "Myrror Security <contact@myrror.security>",
  "description": "Redis store for node-cache-manager",
  "version": "1.0.1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/blindspot-security/node-cache-manager-redis-store.git"
  },
  "scripts": {
    "prepare": "npm prune",
    "test": "jest --forceExit",
    "test-cov": "jest --forceExit --coverage",
    "build": "rm -rf dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "redis": "^4.6.10"
  },
  "devDependencies": {
    "@types/cache-manager": "^4.0.3",
    "@types/node": "^20.8.6",
    "cache-manager": "^5.2.4",
    "jest": "^29.7.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">= 16.18.0"
  },
  "keywords": [
    "cache",
    "redis",
    "redis cache",
    "nestjs redis cache"
  ]
}
